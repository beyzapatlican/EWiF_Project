export class Free {
    question: string;
    solution: string;
    questionNum: number;

    constructor(question: string, solution: string, questionNum: number) {
        this.question = question;
        this.solution = solution;
        this.questionNum = questionNum;
    }
}
