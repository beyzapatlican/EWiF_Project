import { PrepareSessionResponse } from './prepare-session-response.model';

describe('PrepareSessionResponse', () => {
  it('should create an instance', () => {
    expect(new PrepareSessionResponse()).toBeTruthy();
  });
});
